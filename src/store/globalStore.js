// state
const state = {
  // Global State
}

// getters
const getters = {
  // getLoading: (state) => state.isLoading
  // Global Getters
}

// actions
const actions = {
  // Global Actions
}

// mutations
const mutations = {
  // Global mutations
}

export default {
  state,
  getters,
  actions,
  mutations
}
